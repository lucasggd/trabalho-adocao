name: CI/CD - AdotAI (Gradle)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: Build & Test - Spring Boot
    runs-on: ubuntu-latest

    steps:
      - name: 🔽 Checkout do código
        uses: actions/checkout@v4

      - name: ☕ Configurar JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"

      # Se seu Gradle estiver em uma pasta (ex: backend), ajuste aqui
      - name: 🔧 Permitir execução do Gradle Wrapper
        working-directory: ./trab-back
        run: chmod +x gradlew

      - name: 🏗️ Build do projeto
        working-directory: ./trab-back
        run: ./gradlew build -x test

      - name: 🧪 Rodar Testes
        working-directory: ./trab-back
        run: ./gradlew test

      - name: 📊 Resultado
        if: success()
        run: echo "✅ Build e testes concluídos com sucesso!"
